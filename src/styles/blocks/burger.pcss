.burger {
    width: 24px;
    height: 20px;
    flex-direction: column;
    justify-content: space-between;
    display: none;

    @include tablets {
        display: inline-flex;
    }
}

.burger__line {
    display: block;
    background-color: #ffffff;
    height: 4px;
}